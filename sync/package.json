{
  "name": "paklean-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only incremental sync from MongoDB 3.4 primary to local MongoDB secondary.",
  "main": "sync.js",
  "scripts": {
    "start": "node sync.js"
  },
  "dependencies": {
    "mongodb": "^6.10.0",
    "mongodb-legacy-driver": "npm:mongodb@^3.7.4"
  }
}
